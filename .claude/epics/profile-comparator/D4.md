---
name: D4 · End-to-end integration
epic: profile-comparator
stream: D
size: M
owner: Dev 1
agent_type: backend-architect
status: in-progress
created: 2026-04-23T01:24:54Z
updated: 2026-04-23T01:55:28Z
github_issue: 13
depends_on: [A3, B2, C2, C3, D3]
blocks: []
files:
  - app/api/chat/route.ts
  - app/components/chat/**
  - app/components/selectors/**
---

# D4 · End-to-End Integration (Convergence Task)

## 🎯 Objective
Final 30-minute convergence: wire Stream C's inline components into the chat's tool-result rendering, connect `runComparison` → scrape → diff → export end-to-end, handle the empty-diff branch, and demo-test with two real orgs. Closes out **AC-05** and the whole flow.

## 📂 Files
Touches code across streams during integration — but should not *rewrite* anything. Expect small edits only.

## ✅ Acceptance Criteria (→ AC-05 + end-to-end)
- [ ] `validateProfile` tool result renders C2 (if found) or fuzzy-suggestion chips (if not).
- [ ] `Object Settings` selection in C2 triggers `listObjects` → renders C3 inline.
- [ ] C3's `Run Comparison` triggers `runComparison`, which displays loading state, then either:
  - A download link with the correct filename, OR
  - An inline "No differences found between Org A and Org B" message if diff is empty (no file generated).
- [ ] Session-expired error shows the reconnect prompt in-chat.
- [ ] Full run completes in under 30s for a profile with <100 objects (PRD success criterion).

## 🔗 Dependencies
- **Depends on:** A3, B2, C2, C3, D3 — everything else must be merged.
- **Blocks:** nothing — this is the terminal task.

## 📝 Demo Prep
After integration passes, team runs the demo script once end-to-end. Record a backup screen capture in case the live orgs misbehave during presentation.
