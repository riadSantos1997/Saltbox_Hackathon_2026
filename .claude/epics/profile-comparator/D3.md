---
name: D3 · Chat route (streamText + tools)
epic: profile-comparator
stream: D
size: S
owner: Dev 1
agent_type: backend-architect
status: backlog
created: 2026-04-23T01:24:54Z
updated: 2026-04-23T01:24:54Z
github_issue: 12
depends_on: [D1, D2]
blocks: [D4]
files:
  - app/api/chat/route.ts
---

# D3 · Chat Route with Streaming + Tools

## 🎯 Objective
Wire `streamText` with the D2 tools. Orchestrate the happy path (prompt → validate → option selector → scrape → diff → export) and the off-paths (fuzzy suggestions, one-org-missing halt, session expiry).

## 📂 Files
- `app/api/chat/route.ts`

## ✅ Acceptance Criteria
- [ ] POST endpoint returns a streaming response consumable by C1's `useChat()`.
- [ ] System prompt primes the LLM to: (1) call `validateProfile` first, (2) on fuzzy suggestions render them, (3) on single-org match halt with "missing from Org X" message, (4) present the 4-option selector, (5) for Object Settings call `listObjects` then wait for picker confirm.
- [ ] Fuzzy-suggestion response flow — implements **F-01** UX.
- [ ] One-org-missing halt — implements **AC-02**.
- [ ] **Runtime = Node (not edge)** — avoids 10s edge timeout on long scrapes (epic risk).

## 🔗 Dependencies
- **Depends on:** D1 (parser), D2 (tools).
- **Blocks:** D4.

## 📝 Notes
Keep the system prompt short — the tools do the heavy lifting. LLM is a conductor.
